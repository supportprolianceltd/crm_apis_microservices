# Environment Configuration
NODE_ENV=development
PORT=3005

# Database Configuration
DATABASE_URL=postgresql://postgres:password@rostering-db:5432/rostering_dev
POSTGRES_DB=rostering_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_INITDB_ARGS="--encoding=UTF-8"

# Redis Configuration
REDIS_URL=redis://rostering-redis:6379
REDIS_HOSTS=local:rostering-redis:6379
HTTP_USER=admin
HTTP_PASSWORD=admin

# Auth Service Configuration
AUTH_SERVICE_URL=http://auth-service:8001
GATEWAY_URL=http://localhost:9090
JWT_RS256_PUBLIC_KEY_URL=http://localhost:8001/auth/public-key/
# JWT_SECRET=django-insecure-va=ok0r=3)*b@ekd_c^+zkz&d)@*sd3sm$t(1o-n$yj)zwfked

# Geocoding Service
NOMINATIM_URL=https://nominatim.openstreetmap.org

# Default matching radius in meters
DEFAULT_MATCHING_RADIUS=5000

# Email polling interval in minutes
EMAIL_POLLING_INTERVAL=5

# Logging
LOG_LEVEL=debug
LOG_FILE=./logs/rostering.log

# Advanced Optimization
USE_PYTHON_OPTIMIZER=true
PYTHON_OPTIMIZER_URL=http://ortools-optimizer:5000

GOOGLE_MAPS_API_KEY=AIzaSyDJZrf1fjQYrYWsAhoRDMqnERJacy8R3ss

# OR-Tools
PYTHONUNBUFFERED=1
ORTOOLS_LOG_LEVEL=INFO

# Adminer
ADMINER_DEFAULT_SERVER=rostering-db
ADMINER_DESIGN=lucas

# Email Configuration (for each tenant)
# Format: TENANT_<ID>_EMAIL_HOST, TENANT_<ID>_EMAIL_USER, etc.
TENANT_1_EMAIL_HOST=imap.gmail.com
TENANT_1_EMAIL_PORT=993
TENANT_1_EMAIL_USER=your-email@gmail.com
TENANT_1_EMAIL_PASS=your-app-password
TENANT_1_EMAIL_SECURE=true