# Delete the current .env.test file
rm .env.test

# Create a proper .env.test file
cat > .env.test << EOF
# Test Environment Configuration
NODE_ENV=test
PORT=3005

# Database Configuration for Tests
DATABASE_URL="postgresql://postgres:password@localhost:5450/rostering_dev"

# Redis Configuration
REDIS_URL="redis://localhost:6390"

# Test JWT Secret
JWT_SECRET="test-jwt-secret"

# Geocoding Service (mock for tests)
NOMINATIM_URL=https://nominatim.openstreetmap.org

# Test Email Configuration
TENANT_1_EMAIL_HOST=imap.gmail.com
TENANT_1_EMAIL_USER=test@test.com
TENANT_1_EMAIL_PASS=test-password

# Logging
LOG_LEVEL=error
EOF